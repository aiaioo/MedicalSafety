Flask>=3.0
PyMuPDF>=1.24
python-docx>=1.1

# Only needed for db/migrate_json_to_postgres.py (the app itself still reads/
# writes storage/*.json until it's rewired to talk to Postgres).
psycopg2-binary>=2.9
