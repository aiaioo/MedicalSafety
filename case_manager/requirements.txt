Flask>=3.0
PyMuPDF>=1.24
python-docx>=1.1

# PostgreSQL driver -- used by storage.py (the app's persistence layer) and
# by db/migrate_json_to_postgres.py.
psycopg2-binary>=2.9

# Production WSGI server -- see deploy/case-manager.service. Not used by
# local development (`python3 app.py` uses Flask's own dev server instead).
gunicorn>=22.0
